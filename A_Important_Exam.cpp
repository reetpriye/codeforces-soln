// 1201A 
// Author: REET

#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    int n;
    int m;
    int x = 0;
    int marks = 0;
    int flag = 0;
    cin >> n >> m;
    vector<int> hashTable(27);
    vector<int> w(m);
    vector<string> v(n);
    for(int i=0; i<n; i++) {
        cin >> v[i];
    }
    for(int i=0; i<m; i++) {
        cin >> w[i];
    }
    
    return 0;
}