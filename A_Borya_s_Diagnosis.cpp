// 879A 
// Author: REET

#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    int n;
    int tmp;
    cin >> n;
    vector<vector<int>> v(n);
    for(int i=0; i<n; i++) {
        for(int j=0; j<2; j++) {
            cin >> tmp;
            v[i].push_back(tmp);
        }
    }
    while(n--) {
        for(int i=0; i<n; i++) {
            for(int j=0; j<; j++) {
                if(v[i][j])
                
            }
        }
    }
    return 0;
}