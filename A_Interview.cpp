// 631A 
// Author: REET

#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n;
    cin >> n;
    int u[n];
    int v[n];
    for(int i=0; i<n; i++)
        cin >> u[i];
    for(int i=0; i<n; i++)
        cin >> v[i];
    
    return 0;
}