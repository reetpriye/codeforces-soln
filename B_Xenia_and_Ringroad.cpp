// 339B 
// Author: REET

#include <bits/stdc++.h>
using namespace std;

#define FAST_IO ios_base::sync_with_stdio(false); cin.tie(NULL)

typedef long long ll;

int main() {
    FAST_IO;

    int n;
    int m;
    cin >> n >> m;
    vector<int> v(m);
    for(int i=0; i<m; i++)
        cin >> v[i];
    
    return 0;
}