//  1183B
// Author: REET

#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    int T;
    cin >> T;
    while(T--) {
        int n;
        int k;
        cin >> n >> k;
        vector<int> v(n);
        for(int i=0; i<n; i++)
            cin >> v[i];
               
    }
    return 0;
}