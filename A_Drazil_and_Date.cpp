// 515A 
// Author: REET

#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int a;
    int b;
    int s;
    cin >> a >> b >> s;
    if(s==a+b) {
        cout << "Yes\n";
    }
    else {
        
    }
    return 0;
}

// (x + 1, y)
// (x - 1, y)
// (x, y + 1)
// (x, y - 1)